<%- include('partials/header.ejs'); %>
<%- include('body.ejs'); %>

    <div class="parent">
        <p>Trending Artists and Songs</p>
        <div class="child artists">
            <h2>Artists</h2>
            <ol>
                <% name.forEach(name => { %>
                    <li><%=name%></li>    
                <% }); %>
            </ol>
        </div>
        <div class="child songs">
            <h2>Songs</h2>
            <ol>
                <% song.forEach(song => { %>
                    <li><%=song%></li>    
                <% }); %>
            </ol>
        </div>
    </div>

    
</div>

</body>
</html>